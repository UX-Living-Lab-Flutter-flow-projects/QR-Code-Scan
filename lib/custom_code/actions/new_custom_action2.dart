// Automatic FlutterFlow imports
import '/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'index.dart'; // Imports other custom actions
import '/flutter_flow/custom_functions.dart'; // Imports custom functions
import 'package:flutter/material.dart';
// Begin custom action code
// DO NOT REMOVE OR MODIFY THE CODE ABOVE!

import 'package:http/http.dart' as http;
import 'dart:convert';
// Set your action name, define your arguments and return parameter,
// and then add the boilerplate code using the button on the right!

Future<void> newCustomAction2() async {
  // final url = Uri.parse(
  //     'https://www.qrcodereviews.uxlivinglab.online/api/v1/qr-code/?created_by=mohamed788');

  // try {
  //   final response = await http.get(url);

  //   if (response.statusCode == 200) {
  //     final jsonData = json.decode(response.body);

  //     // Clear existing data
  //     // allQRCodeData.clear();
  //     // activeQRCodeData.clear();
  //     // inactiveQRCodeData.clear();

  //     for (final item in jsonData) {
  //       FFAppState().lean = '${jsonData.length}';
  //       FFAppState().testttt.add(item);

  //       // final qrCodeData = QRCodeData.fromJson(item);
  //       // allQRCodeData.add(qrCodeData);

  //       // if (qrCodeData.isActive) {
  //       //   activeQRCodeData.add(qrCodeData);
  //       // } else {
  //       //   inactiveQRCodeData.add(qrCodeData);
  //       // }
  //     }

  //     // setState(() {});
  //   } else {
  //     print('Failed to fetch data. Status code: ${response.statusCode}');
  //   }
  // } catch (e) {
  //   print('Error fetching data: $e');
  // }
}
